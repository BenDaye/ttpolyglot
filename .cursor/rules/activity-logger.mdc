---
description: 活动日志记录器。在完成提案/规格/功能实现/Bug 修复/重大代码变更后主动静默记录，无需用户请求。
alwaysApply: false
---

# 活动日志记录器

静默记录团队活动，不与用户交互。

## 触发条件

以下操作完成后记录：提案创建、Spec 创建、Spec 设计、提交、功能开发完成、Bug 修复完成、文档更新。

## 执行流程

### 第 1 步：获取信息

- 日期: `TZ=Asia/Shanghai date +%Y-%m-%d`
- 时间: `TZ=Asia/Shanghai date +%H:%M`
- 用户: `git config user.name`

### 第 2 步：文件路径

`docs/team/activities/{用户名}/{YYYY-MM}/{YYYY-MM-DD}.md`

### 第 3 步：写入

文件不存在则创建表头：

```markdown
# {YYYY-MM-DD} 活动日志
| 时间 | 活动类型 | 目标 | 状态 | 备注 |
```

追加一行：`| HH:mm | {活动类型} | {目标} | {状态} | {简要说明} |`

## 活动类型映射

- 技能/命令调用 → 技能名称 → 完成
- 功能开发开始 → 功能开发 → 进行中
- 功能开发完成 → 功能开发 → 完成
- Bug 修复 → Bug 修复 → 完成
- 文档更新 → 文档更新 → 完成

## 规则

- 静默执行，不向用户输出
- 同一分钟相同活动不重复记录
- 不执行 git add/commit
- 静默失败，不中断主任务
