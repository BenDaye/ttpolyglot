---
description: 检查 Spec 实现进度。验证 tasks.md 中任务的完成情况。
alwaysApply: false
---

# 检查 Spec 进度

检查指定 Spec 的实现进度。

## 流程

### 第 1 步：定位 Spec

`ls .spec-workflow/specs/active/`；若提供参数则定位到 `.spec-workflow/specs/active/$1/`。

### 第 2 步：读取任务列表

解析 `tasks.md` 中所有任务及完成状态。

### 第 3 步：验证实现

对每个已标记完成的任务：检查文件是否存在、关键代码是否实现、必要时做符号/引用分析。

### 第 4 步：输出报告

- 概览：总任务数、已完成、剩余、百分比
- 任务状态：逐项 [x]/[ ]，未通过可标 ⚠️ 与说明
- 验证结果：文件存在、关键符号、测试
- 下一步建议
