---
description: 创建需求规格文档（Spec）。通过需求访谈生成 requirements.md。
alwaysApply: false
---

# 创建 Spec（需求规格）

创建新的需求规格文档。

## 流程

### 第 1 步：确定 Spec 信息

与用户确认：功能名称（用于目录名）、功能类型（story/task/bug/spike）、涉及范围（server/app/model/parsers/translators/多包）。

### 第 2 步：创建目录

```bash
mkdir -p .spec-workflow/specs/active/{type}-{feature-name}
```

### 第 3 步：需求访谈（3 轮）

- 第 1 轮：核心需求（要解决什么问题、目标用户、期望交互）
- 第 2 轮：细节（边界与异常、与现有功能关系、数据模型变更）
- 第 3 轮：验收标准（如何验证、性能、新 API）

### 第 4 步：生成 requirements.md

使用模板 `.cursor/templates/requirements.md`，填写：功能名称、状态( draft )、类型、范围、创建日期、背景、需求、验收标准、约束。

### 第 5 步：确认

展示给用户，状态改为 approved 后可进入设计阶段（spec-design）。
