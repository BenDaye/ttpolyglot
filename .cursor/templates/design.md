# {功能名称} - 技术设计

## 方案概述
{整体实现方案}

## 变更清单

### 新增文件
| 文件路径 | 用途 |
|----------|------|
| `{路径}` | {描述} |

### 修改文件
| 文件路径 | 修改内容 |
|----------|----------|
| `{路径}` | {变更说明} |

### 数据库变更
- 迁移 #{N}: {描述}

### API 变更
| 方法 | 路径 | 描述 |
|------|------|------|
| `{GET/POST/PUT/DELETE}` | `/api/v1/{路径}` | {描述} |

## 数据模型

```dart
// 新增/修改的模型
@freezed
class {ModelName} with _${ModelName} {
  // ...
}
```

## 数据流
{请求从前端到后端的完整数据流}

## 错误处理
{异常场景和处理策略}

## 技术决策
| 决策点 | 可选方案 | 选择 | 理由 |
|--------|----------|------|------|
| {决策点} | A / B | {选择} | {理由} |
