import 'package:ttpolyglot_model/model.dart';

/// ProjectStats 类型别名（指向 ProjectStatisticsModel）
typedef ProjectStats = ProjectStatisticsModel;

/// ProjectMember 类型别名（指向 ProjectMemberModel）
typedef ProjectMember = ProjectMemberModel;

/// ProjectRole 类型别名（指向 ProjectRoleEnum）
typedef ProjectRole = ProjectRoleEnum;
